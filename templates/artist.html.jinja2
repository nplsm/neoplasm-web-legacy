{% extends "base.html.jinja2" %}

{% block title %}{{ name }} — NEOPLASM{% endblock title%}

{% block main %}
<main>

    <div class="flex">

        <div><img src="{{ static(image) }}"></div>

        <div>
            <h2>{{ name }}</h2>
            
            <ul class="social">
                {% for link in links %}
                    <li><a href="{{ link.href }}" target="_blank">{{ link.name }}</a></li>
                {% endfor %}
            </ul>
        </div>
        
    </div>

    <article>
        {{ description|safe }}
    </article>

</main>
{% endblock main %}
